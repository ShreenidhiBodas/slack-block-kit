import { SURFACES } from '../surfaces/surfaces.type';

/**
 * Enum denoting all the available blocks in block-kit
 */
const enum BLOCKS {
  ACTIONS,
  CONTEXT,
  DIVIDER,
  FILE,
  HEADER,
  IMAGE,
  INPUT,
  SECTION
};

export { BLOCKS };